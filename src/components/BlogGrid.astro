---
const posts = [
  { title: "Face-to-Face: Practicing the Presence", href: "/blog/face-to-face", date: "Aug 2025", cover: "/images/blog/face.jpg" },
  { title: "Deut 32:8–9 and El Elyon", href: "/blog/deut-32", date: "Aug 2025", cover: "/images/blog/deut32.jpg" },
  { title: "Order of Melchizedek: What It Changes", href: "/blog/melchizedek-order", date: "Aug 2025", cover: "/images/blog/melch.jpg" },
];
---
<section class="blog">
  <div class="blog__wrap">
    <div class="blog__head">
      <h2>Latest from the Blog</h2>
      <a class="link" href="/blog">View all →</a>
    </div>
    <div class="blog__grid">
      {posts.map(p => (
        <a class="post" href={p.href}>
          <div class="post__media">
            <img src={p.cover} alt={p.title} loading="lazy" />
          </div>
          <div class="post__meta">{p.date}</div>
          <h3 class="post__title">{p.title}</h3>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
.blog { padding: 3.5rem 1.25rem; }
.blog__wrap { max-width: 1100px; margin: 0 auto; }
.blog__head { display:flex; align-items:baseline; justify-content:space-between; gap:1rem; margin-bottom:1rem; }
.blog__head h2 { margin:0; font-size: clamp(1.4rem, 1.4vw + 1rem, 2rem); }
.link { text-decoration:none; font-weight:600; color:#121212; }
.blog__grid { display:grid; gap:1rem; grid-template-columns: repeat(1, minmax(0,1fr)); }
.post {
  background:#fff; border-radius:16px; overflow:hidden; text-decoration:none; color:inherit;
  box-shadow: 0 10px 26px rgba(0,0,0,.08);
  transition: transform .16s ease, box-shadow .16s ease;
}
.post:hover { transform: translateY(-2px); box-shadow: 0 12px 32px rgba(0,0,0,.12); }
.post__media img { width:100%; height:auto; display:block; }
.post__meta { padding:.75rem 1rem 0; font-size:.9rem; opacity:.65; }
.post__title { padding: .25rem 1rem 1rem; font-size:1.1rem; line-height:1.4; }

@media (min-width: 720px){
  .blog__grid { grid-template-columns: repeat(3, minmax(0,1fr)); }
}
</style>
